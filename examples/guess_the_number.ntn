import "types";
import "io";
import "random::randrange";

func hi(dab) {
  `print(var.dab)`;
}

is_not_win = true;
number = randrange(0, 100);
num_guesses = 0;

while (is_not_win) {
  prompt = "hi: ";
  guess = types::to_int(io::stdin(prompt=prompt, awdj="wdj"));
  if (guess == number) {
    io::print("You Win!");
    is_not_win = false;
  } else if (guess < number) {
    io::print("Too Low");
  } else if (guess > number) {
    io::print("Too High");
  }
}
