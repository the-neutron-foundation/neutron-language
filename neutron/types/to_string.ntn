func to_string(value) {
`try:
    from neutron.builtin_types import StringType
except ModuleNotFoundError:
    from builtin_types import StringType
self.objects["--return--"] = StringType(str(self.objects["value"].value), enter_value=True)`;
}
