get!("io/print");
get!("io/stdin");
get!("types/to_int");


is_not_win = true;
number = 10;

while (is_not_win) {
  print("Enter A Number:");
  guess = to_int(stdin());
  if (guess == number) {
    print("You Win!");
    is_not_win = false;
  } else if (guess < number) {
    print("Too Low");
  } else if (guess > number) {
    print("Too High");
  }
}
